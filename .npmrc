# Transitive tooling dependencies occasionally pin a Node patch version that is
# newer than the one a given workstation happens to have. The workspace's own
# `engines` field documents the supported range; CI installs Node from .nvmrc.
engine-strict=false
strict-peer-dependencies=false
auto-install-peers=true
resolution-mode=highest
